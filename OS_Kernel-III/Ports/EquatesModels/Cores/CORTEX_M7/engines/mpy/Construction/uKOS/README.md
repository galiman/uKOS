# The minimal port for uKOS OS

This port is intended to be a minimal MicroPython port that actually runs.

The Makefile has the ability to build for a Cortex-M CPU.
To build:

  $ make clean
  $ make
